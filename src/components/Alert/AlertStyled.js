import styled from "styled-components";

const AlertStyled = styled.div`
    .alert{
        margin: 0;
        color: crimson;
        font-size: 1rem;
        margin-bottom: -32px;
    }
`

export default AlertStyled;